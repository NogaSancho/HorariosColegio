// Este archivo define la logica para el sistema de cupos, y tambien incluye el componente para adjuntarlo al componente de HorariosForm.jsx.
import React from 'react';

function Cupos({ materia }) {
    const sistemaCupos = () => {
        /*
            Aquí se implementaría la lógica del sistema de cupos.
            Primero se verificaría si la materia tiene cupos disponibles.
            Si no hay cupos, se mostraría un mensaje indicando que no hay cupos disponibles.
            Si hay cupos, se mostraría un mensaje indicando que hay cupos disponibles, y se le restara uno al contador de cupos disponibles.
        */
        return 0;
    }

    return (
        <>

        </>
    );
}

export default Cupos;